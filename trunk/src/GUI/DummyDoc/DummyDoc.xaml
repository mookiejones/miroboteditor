<!-- Removed Focus Color From Extended Grid Splitter-->
    <UserControl
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:miRobotEditor.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	 	xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:gui="clr-namespace:miRobotEditor.GUI"
		xmlns:vm="clr-namespace:miRobotEditor.ViewModel"
        mc:Ignorable="d" x:Name="document" x:Class="miRobotEditor.GUI.DummyDoc"	
         d:DesignHeight="212" d:DesignWidth="428">
    <UserControl.InputBindings>
        <KeyBinding Command="{Binding ToggleGridCommand}" Key="T" Modifiers="Control"/>
    </UserControl.InputBindings>
        <UserControl.Resources>
    </UserControl.Resources>
        <Grid>        
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>
              <gui:Editor  x:Name="data"  Grid.Row="2"  /> 
            <local:ExtendedGridSplitter   x:Name="grid"  HorizontalAlignment="Stretch" Height="20" CollapseMode="Previous"   Loaded="GridLoaded" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalAlignment="Center" Grid.Row="1"/>
            <gui:Editor x:Name="source" Grid.Row="0"  />            
        </Grid>
    </Grid>
</UserControl>
