<Window x:Class="miRobotEditor.Options.OptionsWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:miRobotEditor.ViewModel"
	Title="miRobotEditor.Options" Height="300" Width="300">
	<Grid>
		<Grid.DataContext>
			<vm:OptionsViewModel/>
		</Grid.DataContext>
		<Button Content="Apply" Command="{Binding ApplyCommand,Mode=OneWay}"/>
		<Button Content="OK" Command="{Binding OKCommand, Mode=OneWay}"/>
        <Button Content="Cancel" Command="{Binding CancelCommand,Mode=OneWay}"/>	
	</Grid>
</Window>